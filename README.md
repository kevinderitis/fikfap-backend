# Backend de producción (Node + Mongo + Stream + S3)

## Quickstart
1) `cp .env.example .env` y completa variables.
2) `npm ci`
3) `npm start`

## Health
- `GET /health`

## Seguridad
- Helmet, HPP, CORS restrictivo, rate limits.
